# Grad Research and Application Assistant

A conversational grad-school discovery assistant built on **Open WebUI** that helps students research **faculty**, **programs**, **application deadlines**, and **resume fit** for CS graduate programs. Data is stored in **Elasticsearch** and answers are generated by a connected LLM (OpenAI or Ollama).

> **Setup guides:**
> - [Setup: Ingestion Script](SETUP_INGESTION.md) — populate Elasticsearch indices
> - [Setup: Open WebUI](SETUP_OPEN_WEBUI.md) — run the frontend + backend
> - [Runbook: Adding Tools](RUNBOOK_ADDING_TOOLS.md) — how to add or modify tools

---

## What It Does

The assistant exposes five builtin tools that the LLM calls via native function calling:

| Tool | Description | Data Source |
|------|-------------|-------------|
| `list_faculty_by_area_and_school` | Find faculty at a specific school who work in a given research area | `csrankings_authors` ES index |
| `list_top_programs_by_area` | Rank CS programs by faculty count in a research area | `csrankings_authors` ES index |
| `get_program_deadlines` | Look up application deadlines for a school's programs | `grad_program_deadlines` ES index |
| `filter_programs_by_deadline_and_degree` | Filter programs by deadline range and degree level (MS/PhD) | `grad_program_deadlines` ES index |
| `check_resume_fit` | Score a resume against a target research area (keyword-based, 0–100) | No ES needed (in-memory taxonomy) |
| `get_usnews_top_schools` | Get the top N US News National University rankings for a given year | `usnews_rankings` ES index |
| `compare_schools_by_area` | Compare two schools by faculty strength in a specific research area | `csrankings_authors` ES index |

Example questions:
- "Which faculty at CMU do NLP?"
- "What are the PhD deadlines for Stanford?"
- "Show me top programs in machine learning with deadlines before March 2026."
- "How well does my resume fit computer vision?" *(paste resume text)*
- "What are the top 10 universities in 2026?"
- "Compare CMU and Stanford for security."

---

## Architecture

```
User (Open WebUI chat)
   │
   ▼
Open WebUI Frontend (Svelte)
   │
   ▼
Open WebUI Backend (FastAPI / Python)
   │
   ├── Native function calling → Builtin tool functions
   │       │
   │       ├── Faculty/program tools → Elasticsearch (csrankings_authors)
   │       ├── Deadline tools → Elasticsearch (grad_program_deadlines)
   │       └── Resume fit tool → In-memory keyword taxonomy
   │
   └── LLM (OpenAI API or Ollama) → grounded response
```

The model decides when to call a tool based on the user's question. Tool results are returned to the model as context, and the model generates a natural language answer grounded in the data.

---

## Tech Stack

| Component | Purpose |
|-----------|---------|
| **Open WebUI** | Chat UI + backend (forked, with custom tools) |
| **Elasticsearch** | Stores faculty, program, and deadline data |
| **OpenAI API** or **Ollama** | LLM for generating responses |
| **Python** | Ingestion scripts + backend tools |
| **CSRankings** | Source dataset for faculty and program data |
| **SERP API** | Used for live scraping |

---

## Project Layout

```
.
├── docs/
│   ├── README.md                  # This file
│   ├── SETUP_INGESTION.md         # How to populate Elasticsearch
│   ├── SETUP_OPEN_WEBUI.md        # How to run the app
│   └── RUNBOOK_ADDING_TOOLS.md    # How to add/modify tools
├── scripts/
│   ├── ingestion.py               # CSRankings → ES (csrankings_authors index)
│   ├── ingest_deadlines.py        # Deadline data → ES (grad_program_deadlines index)
│   ├── requirements.txt
│   └── data/
│       └── deadlines.jsonl        # Deadline dataset
├── elastic-start-local/
│   ├── docker-compose.yml         # Local ES + Kibana
│   ├── start.sh / stop.sh
│   └── ...
└── open-webui/                    # Forked Open WebUI with custom tools
    └── backend/
        └── open_webui/
            ├── tools/builtin.py           # Tool implementations
            ├── utils/tools.py             # Tool registration
            ├── utils/grad_school.py       # ES helpers + resume scoring
            ├── utils/middleware.py         # Chat middleware (resume fit injection)
            ├── utils/SearchScore.py        # Scraper for deadline information that is not available
            └── config.py                  # Config (ES URL, index names, feature flags)
```

---

## Elasticsearch Indices

### `csrankings_authors` (faculty + program data)

Aggregated from CSRankings. One document per author with their school, research areas, top venues, homepage, and Google Scholar URL. Used by `list_faculty_by_area_and_school`, `list_top_programs_by_area`, and `compare_schools_by_area`.

Ingested by: `scripts/ingestion.py`

### `grad_program_deadlines` (application deadlines)

One document per program deadline entry. Fields include school, program, degree level, deadline date, term, and source URL. Used by `get_program_deadlines` and `filter_programs_by_deadline_and_degree`.

Ingested by: `scripts/ingest_deadlines.py` (from `scripts/data/deadlines.jsonl`)

### `usnews_rankings` (university rankings)

One document per university ranking entry. Fields include university name, state, IPEDS, year, and rank. Used by `get_usnews_top_schools`.

Ingested by: `scripts/ingest_usnews_rankings.py` (from [`scripts/data/usnews_top10_2020_2026_long.csv`](https://github.com/MarzooqN/CSE-5914-Project/tree/changes/scripts/data/usnews_top10_2020_2026_long.csv))

---

## Configuration

Key environment variables (set in `open-webui/backend/.env` or `open-webui/.env`):

| Variable | Default | Description |
|----------|---------|-------------|
| `ENABLE_GRAD_SCHOOL_TOOLS` | `true` | Enable/disable all grad-school tools |
| `ELASTICSEARCH_URL` | `http://localhost:9200` | Elasticsearch endpoint |
| `ELASTICSEARCH_API_KEY` | *(none)* | API key for ES authentication |
| `GRAD_SCHOOL_ES_INDEX` | `csrankings_authors` | Index for faculty/program data |
| `GRAD_SCHOOL_DEADLINES_ES_INDEX` | `grad_program_deadlines` | Index for deadline data |
| `GRAD_SCHOOL_USNEWS_RANKINGS_ES_INDEX` | `usnews_rankings` | Index for US News rankings |

---

## Quick Start

1. **Start Elasticsearch** — see [SETUP_INGESTION.md](SETUP_INGESTION.md#1-run-a-local-elasticsearch-cluster)
2. **Ingest data** — run `ingestion.py`, `ingest_deadlines.py`, and `ingest_usnews_rankings.py` per [SETUP_INGESTION.md](SETUP_INGESTION.md#5-run-the-ingestion-script)
3. **Start Open WebUI** — see [SETUP_OPEN_WEBUI.md](SETUP_OPEN_WEBUI.md)
4. **Chat** — select a model (e.g. `gpt-4o-mini`) and ask about faculty, deadlines, or resume fit

---

## References

- [Open WebUI docs](https://docs.openwebui.com/)
- [Elasticsearch (Docker)](https://www.elastic.co/docs/deploy-manage/deploy/self-managed/install-elasticsearch-with-docker)
- [CSRankings](https://github.com/emeryberger/CSrankings)
- [Ollama API](https://docs.ollama.com/api/introduction)
